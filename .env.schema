# @defaultSensitive=true @defaultRequired=true
# ---
# No env vars yet. rg found zero process.env / Bun.env / import.meta.env /
# os.environ / os.getenv consumers in this repo (verified 2026-09-06). This
# schema exists so a future env var gets validated from the start.
